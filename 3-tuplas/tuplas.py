# las tuplas son como las listas, nos permiten almacenar diferente tipos de datos, solo que
# las tuplas son inmutables, una vez definida asi queda
# los indices son iguales a la lista
# la tupla sirve para consultar valores

tupla = ("string", 10, 2.5, False, [1, 2, 3], (4, 5, 6))
print(type(tupla))
print(tupla)
